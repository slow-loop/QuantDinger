# Research Journal — 2026-05-10

## Current Objective
Resume the `my-research/AGENT.md` workflow from the latest handoff instead of only reading
the SOP. Latest prior objective was to cross-test `factor_horizontal_reclaim` on ETH/USDT 1D
and then decide whether BTC's OOS failure was coin-specific or universal.

## What Was Accomplished

### 1. Environment recovery
Docker was initially unavailable because the selected `orbstack` Docker context pointed at a
missing socket and no Docker/OrbStack process was running. Started OrbStack and then brought up
the required research services:

```bash
docker compose up -d postgres redis backend
```

Confirmed `quantdinger-backend` started and the `my-research` volume mount is active via the
existing `docker-compose.override.yml`.

### 2. Track A factor cross-test — `factor_horizontal_reclaim` on ETH/USDT 1D
Ran:

```bash
docker exec -w /app quantdinger-backend python3 my-research/scripts/factor_event_tester.py \
  my-research/factors/factor_horizontal_reclaim.py --symbol ETH/USDT --timeframe 1D
```

Result:

| Period | Horizon | N | Hit Rate | Avg Return | Sharpe |
|--------|---------|---:|---------:|-----------:|-------:|
| IS | 30d | 298 | 60.7% | +11.15% | +0.398 |
| OOS | 30d | 32 | 78.1% | +14.39% | +0.751 |

Verdict: ETH cross-test is positive and stable. BTC's earlier OOS failure is not universal.
`horizontal_reclaim` should now be treated as an ETH-specific strategy candidate rather than
a factor to keep only in "watching" status.

Updated `my-research/factors/factor_horizontal_reclaim.py` with a `2026-05-10 run` history entry.
Raw CSV output was written to the tester's default path:

```text
my-research/log/results/event/event_factor_horizontal_reclaim.csv
```

Because the tester's default filename does not include symbol/timeframe and therefore overwrites
the previous raw result for this factor, also saved a symbol-specific copy:

```text
my-research/log/results/event/event_factor_horizontal_reclaim_ETH_USDT_1D_2026-05-10.csv
```

## Known Issues

1. `strategy_evaluator.py` has no CLI parameter override for `@param` values. Testing the journal
   idea "BNB vol_breakout at 3.0x" cannot be done cleanly by passing `--vol-mult 3.0`.
2. `vol_breakout_4h_long.py` is an active ETH champion with locked parameters. Do not mutate that
   file just to test BNB 3.0x; `project.md` marks active strategy edits as a checkpoint.
3. `my-research/project.md` is currently untracked from a prior session; left untouched.

## Next Steps

1. Build an ETH-specific strategy around `horizontal_reclaim`, or a minimal wrapper strategy that
   enters on the reclaim event and exits after a fixed 30-bar horizon / level failure. This is now
   higher priority than BNB 3.0x because the factor cross-test is validated.
2. For BNB vol_breakout 3.0x, either create a new exploratory strategy file with a clear name
   such as `vol_breakout_4h_long_bnb_high_volume.py`, or add a small runner wrapper under
   `my-research/scripts/` that can override strategy `params` without touching active strategy code.
3. If creating the ETH reclaim strategy, follow Track B: write strategy file, commit, run
   `strategy_evaluator.py` with slippage `0.0005` and commission `0.001`, then append the `run`
   history line and compare against existing champions.
