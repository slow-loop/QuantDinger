# AI Agent Handoff Journal

## 🎯 Current High-Level Objective
Professionalizing the QuantDinger platform to support institutional-grade, modular quantitative research. Key goals include alternative data injection (e.g., funding rates), IS/OOS backtest pipelines, and factor-based composite strategies.

## 🟢 Current State (As of 2026-05-03)

### Architecture Decision: Route A (Data Hijacking) — ADOPTED
- Alternative data (e.g., Binance funding rates) is injected at the **Runner level** via `AltDataBacktestService._fetch_kline_data()`.
- Strategies read injected columns directly from `df` (e.g., `df['funding_rate']`).
- Core engine (`backtest.py`) is **NOT modified** — clean CI/CD.
- Strategies gracefully degrade (fallback to `0`) when running outside the Alt-Data Runner (e.g., Web UI).

### What Was Fixed Today
1. **`funding_rate_bottom_fisher.py`** — Removed broken `call_indicator('Factor_Funding_Rate', df)` calls. Now reads `df['funding_rate']` directly (Data Hijacking) and inlines the Z-Score calculation.
2. **`AI_AGENT_SYSTEM_PROMPT.md`** — Complete rewrite:
   - Fixed backtest command (now points to `scratch/backtest_runner.py`)
   - Added Data Hijacking pattern documentation
   - Added Factor Composition guidelines
   - Added IS/OOS evaluation criteria (Alpha > 0 in both)
   - Added realistic cost enforcement (slippage ≥ 0.05%, commission ≥ 0.1%)
   - Added advanced code template

### Assets Inventory
| Type | File | Status |
|------|------|--------|
| Strategy | `research/examples/strategies/funding_rate_bottom_fisher.py` | ✅ Fixed |
| Strategy | `research/examples/strategies/composite_alpha_strategy.py` | ⚠️ Still uses `call_indicator` for research factors |
| Strategy | `research/examples/strategies/sma_cross_rsi_basic.py` | ✅ Works |
| Strategy | `research/examples/strategies/sma_cross_rsi_atr_dynamic_stop.py` | ✅ Works |
| Strategy | `research/examples/strategies/sma_cross_rsi_adx_trend_filter.py` | ✅ Works |
| Factor | `research/examples/factors/factor_funding_rate.py` | ✅ Works (Data Hijacking) |
| Factor | `research/examples/factors/factor_mean_reversion.py` | ✅ Works |
| Factor | `research/examples/factors/factor_trend_momentum.py` | ✅ Works |
| Factor | `research/examples/factors/factor_volatility_squeeze.py` | ✅ Works |
| Runner | `scratch/backtest_runner.py` | ✅ Works (IS/OOS + Alt Data) |
| Service | `app/services/alt_data_caller.py` | ⚠️ Built but NOT injected into sandbox |
| Data | `app/data_sources/alt_data.py` | ✅ Works |

## 🔴 Known Issues / Disconnects
1. **`composite_alpha_strategy.py`** still uses `call_indicator('Factor Trend Momentum', df)` etc. These factors exist only as local files in `research/examples/factors/`, not as platform-registered indicators. This strategy will crash if run through the standard backtest engine. **Fix**: Either inline the factor logic or register the factors in the DB.
2. **`AltDataCaller` service** (`app/services/alt_data_caller.py`) is fully implemented but never injected into `backtest.py`'s `_execute_indicator()` sandbox. This is intentional under Route A (we bypass it via Data Hijacking at the Runner level), but future Route B migration would require this injection.
3. **Dual strategy locations**: `research/examples/strategies/` has 8 strategies while `docs/examples/` has only 3. Need to clarify canonical source.

## ⏭️ Next Steps for the Agent
1. Run a full IS/OOS backtest of `funding_rate_bottom_fisher.py` in Docker to validate the fix.
2. Fix `composite_alpha_strategy.py` (either inline factors or register them).
3. Consolidate strategy file locations (pick one canonical directory).
4. Consider registering the 4 factor modules as platform indicators for Web UI compatibility.
